function red (name) {
    let message = name + 'red';
    console.log(`${message}`);
    return message;
}
function blue (name) {
    let message = name + 'blue';
    console.log(`${message}`);
    return message;
}
function lime (name) {
    let message = name + 'lime';
    console.log(`${message}`);
    return message;
}